# 省两个常驻进程:内核日志直接走 console/dmesg,不再跑用户态 syslogd/klogd
# CONFIG_SYSLOGD is not set
# CONFIG_KLOGD is not set

# 首次启动时在 SD 卡剩余空间上格式化 share 分区用
CONFIG_MKFS_VFAT=y
# S00sdsetup 用 blkid 的 TYPE= 判断是否已有文件系统;默认 busybox 不打印 TYPE
CONFIG_FEATURE_BLKID_TYPE=y
